# Next Task

This file contains exactly one bounded next task.

## Task

**Title:** Authorize an exact feature-branch Pages preview, then physically validate Guided Observation Presets V1

GitHub Actions run `30062308604` built exact feature commit
`288981211d545da36e368161c6b68a51e8c02fd9`, but Pages rejected deployment because its protected
`github-pages` environment does not allow `feature/guided-observation-presets-v1`. Configure or
approve a bounded Pages preview/deployment that accepts that exact feature branch without merging
development `master` (which contains excluded V3B work), then verify the hosted source identifier
before opening its Course-40 URL in a fresh private Quest Browser session. Confirm query gating,
initial defaults, all three preset selections, subtle Observation Orange, manual Modified status,
Restore, both-eye stability, and preservation of grid, planets, Sun, Moon, calibration, observer
state, and simulation time. Record only observed PASS, FAIL, UNCERTAIN, or NOT RUN evidence. Do
not promote stable.

## Recommended execution

- **Model:** GPT-5.6 Terra
- **Reasoning effort:** High
